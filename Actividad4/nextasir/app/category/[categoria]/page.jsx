import Link from "next/link";
export default function Page({ params }) {
 return( 
    <>
        <h1>Pagina {params.categoria}</h1>
        <h1>Pagina {params.categoria}</h1>
        <h1>Pagina {params.categoria}</h1>
        <h1>Pagina {params.categoria}</h1>
    </>
 );
}
